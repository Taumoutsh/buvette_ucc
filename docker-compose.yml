services:
  buvette:
    build: .
    container_name: buvette-ucc
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:${PORT:-4000}"
    volumes:
      - buvette-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production

volumes:
  buvette-data:
